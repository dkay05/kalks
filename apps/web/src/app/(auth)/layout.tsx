/**
 * Auth layout: no marketing chrome. AuthShell (inside each page) provides
 * the split-panel design. Pages here are noindex.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
