# Public (exposed to the browser)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=KALKS

# Sibling apps in this monorepo. Marketing CTAs (login, register,
# dashboard) link here. Use the deployed hostnames in production.
NEXT_PUBLIC_TRADER_URL=http://localhost:3010
NEXT_PUBLIC_ADMIN_URL=http://localhost:3011

# Server-only (never prefixed with NEXT_PUBLIC_)
# MARKET_DATA_API_KEY=
# CRM_WEBHOOK_URL=
# NEWSLETTER_API_KEY=
